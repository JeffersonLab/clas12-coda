/* et_user.c - extract appropriate data from data banks and fills ET user[] array */

int
et_user(unsigned int *data, unsigned int *user)
{
  int i, len;
  /*
  len = data[0];
  for(i=1; i<=len; i++) printf("0x%08x ",data[i]);
  printf("\n");
  */
  return(0);
}
